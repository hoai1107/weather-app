import styled from "styled-components";
import anchor from "./anchors-away.svg";

const Background = styled.div`
  height: 100vh;
  width: 100vw;
  background-image: url(${anchor});
`;

export default Background;